/**
 * Horizontal rhythm aligned with FS service pages (e.g. CCTV commercial): Tailwind `container` + `px-6 lg:px-8`
 * — same pattern as `apx-fs-website` ServicePageHero and section wrappers.
 */
export const MEP_SERVICE_CONTENT_OUTER_CLASS = "container relative mx-auto w-full px-6 lg:px-8"
